{
  "name": "sphinx-bot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "worker-build --release",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
